version: "3.9"
services: 
  web:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - 8080:8080
    volumes: 
      - ./app:/SIDSaaS/app
    env_file: 
      - .env
      - .flaskenv